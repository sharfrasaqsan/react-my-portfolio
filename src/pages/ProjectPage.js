import React from "react";
import ProjectList from "../components/ProjectList";

const ProjectPage = () => {
  return (
    <section>
      <ProjectList />
    </section>
  );
};

export default ProjectPage;
